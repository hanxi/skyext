- 网络协议消息采用 `message_name { request { ... } response { ... } }` 结构，并通过数字编号（如 100、101、102）划分不同业务域的消息 ID。
- 持久化结构体以点号前缀命名（如 `.role`、`.role_bag`、`.mail`），并使用 `_version` 字段标记版本以便后续兼容检查。
- 列表型字段统一使用 `*Type(id)` 形式声明，其中 id 字段作为该条记录的主键引用。
- 每个 schema 文件顶部用 `#` 注释说明该结构体的业务含义，字段也附带简短注释解释用途。