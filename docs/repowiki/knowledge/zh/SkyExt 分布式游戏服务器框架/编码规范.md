- C 扩展统一放在 lualib-src/ 下并通过 build.mk 编译为 luaclib/*.so 供 lualib 调用
- 业务服务按节点类型拆分到 app/account、app/role、app/robot，各自拥有独立的 etc/*.conf.lua 与 etc/app/*.app.lua 配置文件
- 协议与数据模型分离：proto/ 定义网络 sproto schema，schema/ 定义 ORM 数据 schema，均通过 tools 下的生成脚本产出对应 Lua 代码
- Lua 代码风格由根级 .stylua.toml 与 .styluaignore 统一格式化，静态检查由 .luacheckrc 集中配置