初始化流程：`make init` 拉取子模块 → `make build` 编译 skynet 与 C 扩展 → `make proto && make schema && make autocode` 生成协议/ORM/角色代理代码 → 启动 `./bin/skynet etc/account*.conf.lua` / `etc/role*.conf.lua` / `etc/robot.conf.lua`；开发辅助命令包括 `make format`（stylua）、`make lint`（luacheck）、`make dist`（打包）。