- 每个服务进程在 etc/app/*.app.lua 中以 service 表声明，并在对应 etc/*.conf.lua 中提供端口、数据库、etcd 等运行参数
- 跨进程消息统一使用 sproto 协议定义，并通过 tools/gen_proto.sh 自动生成 Lua 绑定
- 服务启动入口遵循 main.lua → global.lua → modules/init.lua 的分层组织，业务逻辑按功能拆分为 modules/* 子模块
- 对外接口通过 lualib 下的 *api.lua 文件封装，供其他服务或客户端模块调用