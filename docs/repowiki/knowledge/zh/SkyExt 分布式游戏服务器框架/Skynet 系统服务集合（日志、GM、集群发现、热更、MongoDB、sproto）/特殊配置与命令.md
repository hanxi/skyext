logger 服务入口必须是 `service/logger/main.lua`，其内部用 `xpcall(require "start", debug.traceback)` 包装启动逻辑，以便在 skynet 日志尚未就绪时将启动失败堆栈写入 `bootfaillogpath` 配置路径；其他服务通过 `cmd_api.dispatch(CMD)` 暴露内部命令，通过 `gm_api.register(GM_CMD)` 暴露 GM 命令。