- 每个测试套件以 `test.conf` 作为 Skynet 启动入口，统一通过 `include "../../etc/common.conf.lua"` 继承公共配置。
- Lua 服务内部采用局部函数定义业务逻辑（如 `test_etcd_service1`），再通过 `service.new` 注册为命名服务（如 `.test-etcd1`）。
- 配置集中通过 `config.get_table("etcd_config")` 获取，避免硬编码连接参数。
- 日志输出统一使用 `log.info(...)` 并以键值对形式记录关键变量（如 `r`, `ret`, `err`）。