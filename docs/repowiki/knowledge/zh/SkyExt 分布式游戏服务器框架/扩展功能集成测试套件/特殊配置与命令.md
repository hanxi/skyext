每个子套件以独立配置文件运行：`lua -e 'require "skynet"; require "conf"' test/test_etcd/test.conf` 或 `test/test_jwt/test.conf`，其中 `test.conf` 通过 `include "../../etc/common.conf.lua"` 复用公共配置，并通过 `luaservice = luaservice .. root .. "test/?/main.lua;"` 动态注册测试服务。