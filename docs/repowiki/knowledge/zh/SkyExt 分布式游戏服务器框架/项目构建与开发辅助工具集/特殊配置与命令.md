所有 shell 脚本均先 `cd "$(dirname "$0")/.."` 切到仓库根目录后再执行，因此需从仓库根目录调用；`dist.sh` 会清空并重建 `./dist` 目录最终产出 `skyext.zip`；`gen_schema.sh` 依赖 `stylua` 对生成的 schema 文件进行格式化；`hotfix.sh refresh <dir>` 要求宿主机存在 `md5sum` 或 `md5` 命令。