- Lua 资源文件统一使用 return { ... } 导出纯数据表，键名采用大驼峰风格（如 Height、MountId、HitCorrection）。
- GM 面板前端将样式、脚本全部内联于单个 HTML 文件中，不依赖外部 JS/CSS 库，便于独立部署。
- 前端通过 fetch 调用 /gm/list 与 /gm/execute 两个固定路由，请求/响应均遵循统一的 JSON 格式（code、error、data、results）。